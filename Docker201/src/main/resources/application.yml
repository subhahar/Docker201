spring :
  datasource :
    url : jdbc:mysql://localhost:3306/shopingcart
    username : root
    password : admin
    driver-class-name: com.mysql.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        use-new-id-generator-mappings: false
